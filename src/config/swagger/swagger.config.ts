export interface SwaggerConfig {
  title: string;
  description: string;
  version: string;
}

export const swaggerConfig: SwaggerConfig = {
  title: 'LogFood Core Service API',
  description: 'Arquitectura base sin endpoints funcionales',
  version: '0.1.0'
};
